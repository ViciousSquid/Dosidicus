# Only these plugins load (and auto-enable) at startup.
# Add one plugin per line. Lines starting with # are ignored.
# NOTE: each name is the plugin's PLUGIN_NAME lowercased, NOT the folder name.
#   folder plugins/achievements -> PLUGIN_NAME "Achievements" -> whitelist "achievements"
# (multiplayer is intentionally omitted; it is also blacklisted from auto-start.)
#
# STDP and Sleep Replay used to be listed here. Both are core engine features
# now and always on - STDP through PlasticityEngine, consolidation through
# BrainWidget's ConsolidationManager - so there is nothing left to enable.
achievements
