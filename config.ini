[General]
language = en

[Debug]
multiplayer_debug = False

[Compute]
backend = numpy

[Display]
neuron_label_font_size = 8
neuron_radius = 14
connection_line_width = 1.5
button_font_size = 16
button_width = 140
button_height = 50
button_spacing = 20

[RockInteractions]
pickup_probability = 0.7
throw_probability = 0.4
min_carry_duration = 4.0
max_carry_duration = 10.0
cooldown_after_throw = 10.0
happiness_boost = 9
satisfaction_boost = 11
anxiety_reduction = 9
memory_decay_rate = 0.98
max_rock_memories = 10

[Neurogenesis]
enabled = True
showmanship = True
pruning_enabled = True
cooldown = 60.0
per_type_cooldown = 30.0
max_novelty_neurons = 5
pattern_threshold = 3
experience_buffer_size = 50
min_utility_for_keep = 0.2
max_neurons = 128
initial_neuron_count = 7
max_hebbian_pairs = 2

[Neurogenesis.Novelty]
enabled = True
# Was 3.0. novel_objects decays x0.95 per tick, so exactly three novel objects
# gave a counter of 3.0 - which is not > 3.0 - and the trigger was above
# threshold for zero ticks. Novelty neurons were effectively unreachable while
# stress neurons spawned on any startle.
threshold = 2.0
decay_rate = 0.85
max_counter = 10.0
min_curiosity = 0.3
adventurous_modifier = 1.2
timid_modifier = 0.8

[Neurogenesis.Stress]
enabled = True
threshold = 2.0
decay_rate = 0.85
max_counter = 10.0
min_anxiety = 0.4
timid_modifier = 1.5
energetic_modifier = 0.7

[Neurogenesis.Reward]
enabled = True
threshold = 2.5
decay_rate = 0.85
max_counter = 8.0
min_satisfaction = 0.5
boost_multiplier = 1.1

[Neurogenesis.SpecialisationCaps]
novelty_object_investigation = 8

[Neurogenesis.NeuronProperties]
base_activation = 0.5
position_variance = 75
default_connections = True
connection_strength = 0.35
reciprocal_strength = 0.15
randomize_start_positions = True
canvas_padding = 60
centering_force = 0.02
force_bounds = True

[Neurogenesis.VisualEffects]
highlight_duration = 5.0
highlight_radius = 40
pulse_effect = True
pulse_speed = 0.5
animation_style = pattern_1

[Neurogenesis.Appearance]
novelty_color = 255,255,150
stress_color = 255,150,150
reward_color = 150,255,150
novelty_shape = triangle
stress_shape = square
reward_shape = circle

[Hebbian]
# Values chosen by simulating a meaningful pair (covariance 0.16) against the
# noise floor over 30 simulated minutes, with the real pair rotation in place.
# The previous settings separated signal from noise by +0.020 (i.e. not at
# all); these separate it by roughly +0.30.
#
# NOTE: this section had no effect at all before 2.6.5.1 - LearningConfig
# never read it, so base_learning_rate = 0.20 actually ran as 0.1.

# Seconds between plasticity commits. Co-activation is accumulated every tick,
# so this is a pacing choice, not a correctness one.
learning_interval = 20

# Applied to the CORRELATION between the two neurons over the window, not to
# an instantaneous product. Setting this equal to weight_decay makes a synapse
# converge to exactly that correlation, so every weight in the network reads
# as a statement about the squid's experience.
base_learning_rate = 0.04

# Sets the time constant: tau = 1/decay = 25 commits, so a synapse settles
# over roughly 8 minutes of play. Keep equal to base_learning_rate.
weight_decay = 0.04

# Weights are SIGNED. Anti-correlated neurons develop inhibitory synapses,
# which is what an avoidance behaviour ("afraid of poop") actually is.
min_weight = -1.0
max_weight = 1.0

# Floor on pairs committed per cycle; the engine also takes half the candidate
# pool, so coverage grows with the brain instead of thinning out. At the old
# fixed 2, a synapse in a 20-neuron brain only updated every 47 minutes.
max_hebbian_pairs = 4

# Blend between rules: 0.0 = pure Hebbian, 1.0 = pure spike timing.
# STDP is a core feature, not a plugin.
stdp_weight = 0.4

[LinkBlink]
interval_min = 8.0
interval_max = 20.0
blink_duration = 2.0

[Animation]
style = vibrant

[Designer]
designer_min_neuron_distance = 90
designer_max_neuron_distance = 200

[Facts]
enabled = True
interval_minutes = 6
display_seconds = 12

[Sleep]
recovery_per_second = 28.0
happiness_boost = 0.45
satisfaction_boost = 0.35
sink_speed = 0.58
bottom_padding = 25
bob_amount = 1.2


